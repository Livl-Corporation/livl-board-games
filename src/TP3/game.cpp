#include "game.hpp"

void Game::nextRound()
{
    round++;

    // Determines who is playing this round

    // Ask him in which cell he wants to place his cell

    // Place his symbol then display grid

    // Verify if player has won

    // Verify if grid is full

    // Otherwhise, next round.
}

bool Game::hasWon(Symbol symbol) const
{
    // if max consecutive player symbols > consecutiveSymbolsToWin, he has won !
}