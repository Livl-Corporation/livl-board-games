//
// Created by Julien on 18/01/2023.
//

#include "checkersToken.hpp"

CheckersTokenType CheckersToken::getComputerTokenType(PlayerId playerId) const {
//    if (playerId == 1) {
//        return CheckersTokenType::PLAYER_ONE;
//    } else {
//        return CheckersTokenType::PLAYER_TWO;
//    }
//
//    if(isKing(position))
//        return (playerId == 1) ? TokenType::X_King : TokenType::O_King;
//    else
//        return (playerId == 1) ? TokenType::X : TokenType::O;
}
